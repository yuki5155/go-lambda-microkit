/// <reference types="vite/client" />

declare module 'axios' {
    export interface AxiosInstance {
      // ここに AxiosInstance の型定義を追加
    }
    export interface AxiosError {
      // ここに AxiosError の型定義を追加
    }
  }